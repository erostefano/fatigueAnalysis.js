const eyesOpen = {
    labels: 'eyes-open',
    encoding: [1, 0, 0]
};

const eyesClosed = {
    labels: 'eyes-closed',
    encoding: [0, 1, 0]
};

const yawning = {
    labels: 'yawning',
    encoding: [0, 0, 1]
};

module.exports = {eyesOpen, eyesClosed, yawning};
